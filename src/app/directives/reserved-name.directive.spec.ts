import { ReservedNameDirective } from './reserved-name.directive';

describe('ReservedNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ReservedNameDirective();
    expect(directive).toBeTruthy();
  });
});
